# Data Flow

`FinanceDataHub -> weekly panel -> qlib workflow -> score snapshot / portfolio targets -> sync -> ValueInvesting consumer`

未来 UI 加入后，数据流仍不改变，只是在 workflow 触发和结果浏览层增加 API + Web workbench。
