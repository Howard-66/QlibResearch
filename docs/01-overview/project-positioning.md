# Project Positioning

`QlibResearch` 是 TradingNexus 体系下的独立研究工作台仓库，负责 qlib 研究、工作流生成、结果复盘、模型发布与向 `ValueInvesting` 交付研究产物。

当前阶段的核心原则：

- 研究仓独立运行，不 import `ValueInvesting/src/*`
- 在线消费继续由 `ValueInvesting` 负责
- 先稳定 CLI 和 Notebook，再演进到独立 API + Web UI
